window.addEventListener('scroll', function () {

    const parallax3 = this.document.querySelector('.column3');
    let scrollPosition = window.pageXOffset;


    parallax3.style.transform = 'translateX(' + scrollPosition * -.3 + 'px)';
    
});

window.addEventListener('scroll', function () {

    const parallax1 = this.document.querySelector('.meander1');
    let scrollPosition = window.pageXOffset;


    parallax1.style.transform = 'translateX(' + scrollPosition * -1.5 + 'px)';
    
});

window.addEventListener('scroll', function () {

    const parallax2 = this.document.querySelector('.meander2');
    let scrollPosition = window.pageXOffset;


    parallax2.style.transform = 'translateX(' + scrollPosition * -2.5 + 'px)';
    
});

window.addEventListener('scroll', function () {

    const parallax4 = this.document.querySelector('.column4');
    let scrollPosition = window.pageXOffset;


    parallax4.style.transform = 'translateX(' + scrollPosition * -.3 + 'px)';
    
});

window.addEventListener('scroll', function () {

    const parallax5 = this.document.querySelector('.column5');
    let scrollPosition = window.pageXOffset;


    parallax5.style.transform = 'translateX(' + scrollPosition * -.3 + 'px)';
    
});

window.addEventListener('scroll', function () {

    const parallax6 = this.document.querySelector('.column6');
    let scrollPosition = window.pageXOffset;


    parallax6.style.transform = 'translateX(' + scrollPosition * -.3 + 'px)';
    
});