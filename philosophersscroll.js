window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const plato = this.document.querySelector('.plato');

    console.log(scrollPosition);

    if (scrollPosition > 1752) {
        plato.style.animation = "platoentry ease 1s forwards";
        plato.style.animationDelay = ".3s";
    }
});


window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const platoquote = this.document.querySelector('.plato-quote');


    if (scrollPosition > 1752) {
        platoquote.style.animation = "platoquoteentry ease 1s forwards";
        platoquote.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const platobio = this.document.querySelector('.platobio');


    if (scrollPosition > 1752) {
        platobio.style.animation = "platobioentry ease 1s forwards";
        platobio.style.animationDelay = ".8s";
    }
});










window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const aristotle = this.document.querySelector('.aristotle');


    if (scrollPosition > 2806) {
        aristotle.style.animation = "aristotleentry ease 1s forwards";
        aristotle.style.animationDelay = ".3s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const aristotlequote = this.document.querySelector('.aristotle-quote');


    if (scrollPosition > 2806) {
        aristotlequote.style.animation = "aristotlequoteentry ease 1s forwards";
        aristotlequote.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const aristotlebio = this.document.querySelector('.aristotlebio');


    if (scrollPosition > 2806) {
        aristotlebio.style.animation = "aristotlebioentry ease 1s forwards";
        aristotlebio.style.animationDelay = ".8s";
    }
});















window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const epicurus = this.document.querySelector('.epicurus');


    if (scrollPosition > 4400) {
        epicurus.style.animation = "epicurusentry ease 1s forwards";
        epicurus.style.animationDelay = ".3s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const epicurusquote = this.document.querySelector('.epicurus-quote');


    if (scrollPosition > 4400) {
        epicurusquote.style.animation = "epicurusquoteentry ease 1s forwards";
        epicurusquote.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const epicurusbio = this.document.querySelector('.epicurusbio');


    if (scrollPosition > 4400) {
        epicurusbio.style.animation = "epicurusbioentry ease 1s forwards";
        epicurusbio.style.animationDelay = ".8s";
    }
});















window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const zeno = this.document.querySelector('.zeno');


    if (scrollPosition > 5636) {
        zeno.style.animation = "zenoentry ease 1s forwards";
        zeno.style.animationDelay = ".3s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const zenoquote = this.document.querySelector('.zeno-quote');


    if (scrollPosition > 5636) {
        zenoquote.style.animation = "zenoquoteentry ease 1s forwards";
        zenoquote.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const zenobio = this.document.querySelector('.zenobio');


    if (scrollPosition > 5636) {
        zenobio.style.animation = "zenobioentry ease 1s forwards";
        zenobio.style.animationDelay = ".8s";
    }
});















window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const marcus = this.document.querySelector('.marcus');


    if (scrollPosition > 8409) {
        marcus.style.animation = "marcusentry ease 1s forwards";
        marcus.style.animationDelay = ".3s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const marcusquote = this.document.querySelector('.marcus-quote');


    if (scrollPosition > 8409) {
        marcusquote.style.animation = "marcusquoteentry ease 1s forwards";
        marcusquote.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const marcusbio = this.document.querySelector('.marcusbio');


    if (scrollPosition > 8409) {
        marcusbio.style.animation = "marcusbioentry ease 1s forwards";
        marcusbio.style.animationDelay = ".8s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const thanasis = this.document.querySelector('.thanasis');


    if (scrollPosition > 9716) {
        thanasis.style.animation = "thanasisentry ease 1s forwards";
        thanasis.style.animationDelay = ".3s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const thanasisbio = this.document.querySelector('.thanasisbio');


    if (scrollPosition > 9716) {
        thanasisbio.style.animation = "thanasisbioentry ease 1s forwards";
        thanasisbio.style.animationDelay = ".6s";
    }
});

window.addEventListener('scroll', function () {
    let scrollPosition = window.pageXOffset;

    const thanasissoc = this.document.querySelector('.thanasissoc');


    if (scrollPosition > 9716) {
        thanasissoc.style.animation = "thanasissocentry ease 1s forwards";
        thanasissoc.style.animationDelay = ".9s";
    }
});
